import { useBaseStore } from "../store";

export async function checkCollegeEmail(authuser: string) {
  // Disabled background image probing to prevent console CORS and ERR_TOO_MANY_REDIRECTS warnings
  return;
}
